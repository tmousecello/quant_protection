# 論文草稿(依 advisor 結構公式;繁體中文)

> 用途:給 advisor 逐段批註的初稿。數字全部來自已實測結果(Stage 0–2);標【待定】處需要團隊/advisor 決定;標【圖】處為預定圖位。第三、四章依公式先立骨架、文繞圖,細節句可再長。

---

## Abstract(依公式四段)

【第一句:背景 + challenge + motivation】
記憶體已成為向量資料庫最昂貴的資源,而 DRAM 產能短缺預期將持續數年;同一批晶圓中其實存在大量僅因無法滿足「零錯誤」出廠門檻而被淘汰的顆粒——若能打撈這些高錯誤率記憶體來承載記憶體飢渴的向量檢索,將直接開啟一批新的低成本供給,但前提是:向量索引必須能在會反覆出錯的記憶體上維持檢索品質。

【第二句:與以前 work 不同】
不同於既有的均勻加固方法(硬體 ECC、資料庫查詢處理的 AN-coding 加固)對每個位元組支付相同的保護成本,我們實測發現向量索引的每位元危險度橫跨六個數量級,因此保護預算應按危險度、而非按位元組數分配。

【第三句:方法特點 + enable】
我們提出以危險度為軸的分層保護:對僅 64 B、翻 6 個位元即令 recall 沉默崩潰的全域旋轉結構,以複本多數決搭配自我 scrub 修復——我們實測證明,在錯誤反覆發生的記憶體上,不自我 scrub 的複本是一次性保險絲(首次失效 median 僅 ~21 個錯誤週期,N=30),而事件觸發的 256 B 重載使其可再生;對佔索引 99.9% 的碼資料,以 CRC 偵測搭配 error-bound-aware 降級檢索,在不重載硬碟的情況下容忍錯誤累積——災難防護僅需 132 B(4.7×10⁻⁵%),偵測層開銷依 CRC 粒度為 0.03–5.7% 且可解析地權衡,使向量檢索得以運行於無法通過零錯誤門檻的「淘汰矽」上。

【第四句:實驗與結果】
在真實 RaBitQ-HNSW 索引(SIFT1M)上的故障注入實驗顯示:無保護時 6 個位元錯誤(佔索引 2.7×10⁻⁶%)即令 recall 由 0.984 沉默崩潰至 0.423,而我們的機制以 132 B 的複本成本使防護可再生(200 個錯誤週期內 4 次多數決失效全數由觸發式重載復原、全程 recall 無損);偵測層將可容忍腐蝕提高 29 倍,error-bound 降級再免費放大安全 scrub 間隔 6–23%。

---

## 1. Introduction(依公式五段)

**第一段(背景、趨勢、問題、我們要研究什麼)**
向量資料庫是大型語言模型時代檢索基礎設施的核心,其近似最近鄰(ANN)索引為了查詢延遲必須常駐記憶體,使記憶體成為部署成本的主宰項。與此同時,DRAM 供給正處於長期短缺:產能擴充需時三至五年,而買方對顆粒的要求實質上是「零錯誤」——同一批晶圓中錯誤率偏高、卻仍大部分可用的顆粒因此被整批淘汰。這與 CPU 產業行之有年的 binning(關閉壞核心、降級銷售)形成鮮明對比:binning 得以成立,需要缺陷可定位、且工作負載能繞開缺陷;DRAM 的弱胞(weak cell)地圖在出廠測試即可取得,缺的是後者——一個能繞開記憶體缺陷的重要工作負載。本文研究的問題即是:**向量索引能否成為這個工作負載?**亦即,在會反覆出錯的記憶體上,ANN 檢索的品質(recall)如何被錯誤影響、又能以多低的成本被保護。

**第二段(背景知識與大範圍 related work)**
記憶體錯誤在資料中心是常態而非異常:大規模量測顯示 DRAM 錯誤率遠高於一般假設,且錯誤與胞的物理缺陷相關、會在同一位置反覆發生。傳統防線是硬體 ECC(SECDED 乃至 Chipkill),以約 12.5% 的均勻空間開銷透明地糾正單位元錯誤;近似計算社群則從另一端探索:Flikker 將 DRAM 依資料重要性分區、對非關鍵區降低 refresh 頻率,近似儲存工作亦證明部分應用資料可容忍錯誤以換取成本。這些工作確立了「錯誤率—成本」可以交易,但交易的前提是知道**哪些資料可以容錯**——而這在向量索引上從未被系統性量測。

**第三段(直接相關 related work,以「保護策略」分類;由遠而近)**
以保護策略為軸,既有工作可分為四類。第一類是**硬體均勻保護**:ECC/Chipkill 對所有位元一視同仁,無法利用資料間的重要性差異,且其設計點是通過出廠篩選的正常顆粒(約 10⁻⁶ 級錯誤率)——被 binning 淘汰的顆粒(錯誤率可達 10⁻³)超出其假設。第二類是**軟體均勻編碼**:以 AHEAD 為代表的 AN-coding 加固,將欄位資料乘上常數 A 存放、查詢直接於編碼域運算,使錯誤在查詢處理**過程中**即可被偵測,並可依錯誤率調整 A 的強度;然而其「可調」的維度是錯誤率而非資料重要性——整個欄位付同一份編碼開銷,且其目標是精確關聯語意下的計算正確性,偵測之後的恢復交由上層(重跑、備援)。第三類是**其他領域的危險度感知保護**:DNN 領域已發現權重的指數位元遠比尾數位元危險(Terminal Brain Damage),並發展出選擇性防護(RADAR 等);Meta 的 PVF 量測了嵌入表的錯誤容忍度——但這些工作或止於量測、或作用於模型權重,皆未觸及 ANN **索引結構**。第四類、與本文最近的是**向量資料庫的可靠性實務**:以 Milvus 為代表的生產系統在儲存層具備 checksum,但記憶體服務層無逐查詢檢查、偵測到錯誤的恢復手段是整段自儲存重載——換言之,現行實務隱含假設「記憶體不出錯;若出錯,錯誤是罕見災難,以慢速重載處理」。本文將挑戰的正是這組假設:在打撈爛矽的場景裡,錯誤是持續的天氣而非罕見災難,且我們將證明,均勻保護(第一、二類)在向量索引上浪費了 99.9% 的開銷,而現行實務(第四類)對最危險的錯誤完全不可見。

**第四段(我們的方法、與傳統不同處、enable、實驗摘要;≈第三四章 summary)**
本文首先對真實 RaBitQ 索引進行逐位元故障注入刻畫,發現危險度極端不對稱:全域共享的旋轉結構僅 64 B,單一位元翻轉平均即損失 0.17–0.20 的 recall、且 100% 為無任何外顯徵兆的沉默錯誤;而佔索引主體的量化碼(96 MB)單位元傷害僅約 3×10⁻⁷——**每位元危險度橫跨六個數量級**。基於此,我們提出按危險度分配的分層保護:(i) 對懸崖型結構(旋轉)以三複本多數決於每次查詢修復:實測顯示無防護時 6 位元即沉默全崩;複本多數決可修復,但若複本自身不被 scrub,在反覆錯誤的記憶體上為**一次性保險絲**(首次失效 median ~21 個錯誤週期,N=30);事件觸發的全量重載(自持久 clean 源,256 B)使防護可再生——200 週期內 4 次失效全數復原、recall 全程無損,災難防護成本僅 132 B。(ii) 對斜坡型結構(量化碼)以 CRC 於查詢取用時搭便車偵測,偵測到損壞即降級至索引原生常駐的 1-bit 層、以未損壞的誤差界作悲觀排序(error-bound-aware fallback),使系統在錯誤累積期間持續服務、僅在累積逼近容忍線時批次重載——偵測使可容忍腐蝕提高 29 倍,error-bound 降級再將安全 scrub 間隔免費放大 6–23%。這套機制 enable 的是:以災難防護僅 132 B(4.7×10⁻⁵%)、偵測層依 CRC 粒度 0.03–5.7% 的分層開銷,讓向量檢索運行於錯誤率高達 10⁻³ 的淘汰記憶體上,並可進一步結合出廠弱胞地圖,將 64 B 臨界結構定位於已知良好的胞——保護預算與記憶體品質雙雙按危險度配置。

**第五段(章節安排)**
第二章說明背景並以量測建立動機;第三章呈現分層保護機制的設計;第四章(分析)推導 scrub 間隔與錯誤率的解析關係;第五章為實驗評估;第六章總結。【依最終章節數調整】

---

## 2. Background and Motivation

### 2.1 Background(每個背景一段;【圖】系統架構)

**RaBitQ 多精度索引結構。**RaBitQ 對每個向量存放兩個實體分離的精度層:1-bit 符號碼(bin,約 28 B/向量)供圖遍歷的快速距離估計,與擴展位元碼(ex,b=7 時約 96 B/向量、佔索引主體)供重排序精修;每向量另附誤差界因子,對 1-bit 估計提供數學保證的距離上界。所有向量共享一個全域隨機旋轉:實作上為 FHT+Kac 結構化旋轉的符號向量(64 B / 512 bits),編碼與查詢兩端必須使用同一份,否則基底錯位、所有距離估計失真。【圖 1:索引記憶體佈局與各結構大小,標出 64 B 旋轉 vs 96 MB ex 的懸殊】

**生產系統的可靠性現況。**以 Milvus 2.6(原生支援 IVF_RABITQ)為例:物件儲存層的 segment 具完整性檢查,但索引載入記憶體後的服務期間無逐查詢檢查;錯誤(若被發現)的恢復手段是自儲存重載整個 segment。此設計隱含「記憶體由 ECC 保證不出錯」的假設。

**Binned DRAM 的錯誤特性。**弱胞在出廠測試可定位、空間上固定、且會在同一位置反覆出錯(retention 失效與電荷洩漏方向相關)。因此在打撈場景中,錯誤模型是**持續、反覆、可累積**的——被修復的位元會再次損壞,任何一次性的加固都會隨時間失效。

### 2.2 Observation and Motivation(每個 issue 一段;結尾依公式收)

**Issue 1:向量索引的每位元危險度橫跨六個數量級,均勻保護必然浪費。**我們對真實 RaBitQ-HNSW 索引(SIFT1M,clean recall@10 = 0.98376)進行逐結構、逐位元的故障注入:旋轉結構單位元平均 ΔRecall 0.17–0.20、100% 有害;量化碼 ex 單位元傷害約 3×10⁻⁷、幾乎全數無害;圖連結與叢集編號的損壞以可偵測的越界呈現、對 recall 幾乎無影響。同樣一個位元,危險度取決於它住在哪個結構——而 ECC 與 AN-coding 對它們支付完全相同的成本。【圖 2:F1 危險度地圖,對數座標呈現六個數量級】

**Issue 2:最危險的錯誤是完全沉默的,現行「錯誤即重載」實務對它不可見。**時序累積實驗顯示:無保護時,旋轉累積僅 6 個位元錯誤(佔整個索引 2.7×10⁻⁶%)即令 recall 自 0.984 崩至 0.423,越過沉默崩潰線;全程無 crash、無 NaN/Inf,QPS 與錯誤率等服務指標完全正常——只有答案錯了。依賴「偵測到異常再重載」的現行實務,在這種失效面前沒有觸發點。【圖 3:無保護 recall-over-time,tick-0 崩】

**Issue 3:近似檢索的容錯語意是尚未被利用的自由度。**與關聯查詢的精確語意不同,ANN 的 recall 本非 1.0,錯誤的影響是統計性、可量測、且高度不均的——這正是「繞開缺陷」得以成立的空間;但它從未被既有保護方法(為精確計算設計的 ECC/AN-coding)利用。

**Motivation 收尾段(依公式)。**This work is motivated by the opportunity of salvaging binned DRAM for memory-hungry vector search, with considering 錯誤在打撈場景中持續、反覆、於固定弱胞累積的特性。In contrast to uniform hardening, we are interested in allocating protection budget by measured per-bit criticality rather than by bytes. The technical problems are:(1) 如何以近乎免費的成本守住 64 B 但一觸即沉默崩潰的懸崖型結構——且在錯誤反覆發生下防護本身不退化;(2) 如何讓 99.9% 的斜坡型資料在錯誤累積期間持續服務、將昂貴的重載壓到最低;(3) 偵測與計數如何搭載於查詢路徑而不引入背景掃描開銷。同時需考量:恢復動作必須非破壞性(我們將展示偵測後選錯 policy 比不偵測更糟)、以及所有機制須與現有索引結構相容而非重新設計索引。

---

## 3. 方法:按危險度分層的保護與恢復(依公式:3.1 overview 先總後分;動作導向;pseudo code)

### 3.1 Overview

**第一段(方法、objective、main idea、架構)。**我們提出 criticality-tiered protection:objective 是在給定 recall 底線 R 下最小化保護成本(記憶體 + scrub 開銷),main idea 是保護預算跟著實測危險度走——懸崖型結構(小、沉默、硬閾值)採主動、每查詢、就地修復;斜坡型結構(大、可偵測、緩降)採被動、搭便車偵測、累積容忍、批次重載;免疫結構不設防。整體架構為三層:複本多數決層(旋轉)、CRC + error-bound 降級層(ex)、越界檢查層(圖/指標)。【圖 4:架構圖】

**第二段(以一次查詢的動作串流串起所有元件)。**一次查詢進入時,首先以三複本多數決驗證並修復旋轉(64 B 比對,亞微秒;複本不一致位元數即為精確的錯誤計數);查詢向量以修復後的旋轉編碼、經 1-bit 層遍歷圖收集候選;重排序取用某候選的 ex 區塊前驗其 CRC——通過則以精確距離重排,失敗則不重載硬碟,改以該向量常駐的 1-bit 碼與未損壞的誤差界做悲觀上界排序(僅當最壞情況仍足以進入 top-k 才採納),同時將該區塊記入損壞集、累加損壞計數;當「被取用向量中的損壞比例」逼近容忍線,以群組 I/O 批次重載損壞區塊並歸零計數。圖遍歷全程對連結與編號做越界檢查,越界即跳過而不丟棄。以下各節依序展開三層。

### 3.2 懸崖層:複本多數決與自我 scrub
(動作導向敘述 + pseudo code。重點:每查詢修使損傷不跨期累積;**關鍵設計原因**——為何複本自身也要 scrub:實測顯示若複本與主體同速累積損傷,首次多數決失敗後、因「絕不以錯誤多數覆寫資料」的語意,複本永不再同步,防護淪為一次性保險絲(N=30 分布:median 21、IQR [9, 30]、最早 tick 0;失效後沿無防護軌跡崩落);故成功路徑外增加「複本對 clean 源(索引檔中的靜態旋轉,64 B 讀取近乎免費)定期驗證重載」。此節呼應 2.2 Issue 2 與「錯誤反覆」的環境假設。)

### 3.3 斜坡層:搭便車偵測、error-bound 降級、計數與批次重載
(重點與原因:為何 on-access 而非背景掃描——旋轉全域每查詢必用故 on-access 即完整覆蓋,ex 的損壞比例因錯誤與存取無關而為無偏估計、且「被取用者的損壞比例」正是驅動 recall 的量,偵測直接吐出決策變數;為何 fallback 用悲觀上界——naive 1-bit 降級會製造 false positive、隨候選池擴大反而衰退;為何批次重載——單次 I/O 延遲由尋址主導、與大小近乎無關,群組化把 N 次延遲攤成一次。補洞句:此估計的前提是損壞與存取無關,對 retention/宇宙射線成立,對針對熱資料的對抗性 rowhammer 不成立——列為範圍外。)

### 3.4 越界檢查層與整體成本
(圖/指標損壞以 crash 呈現、對 recall 無害,故僅需最便宜的越界跳過;彙總三層成本:懸崖端 132 B(增量複本 128 B + CRC 4 B,4.7×10⁻⁵%)+ 斜坡端每元素 CRC manifest(依粒度 0.03–5.7%)+ 每查詢亞微秒投票——災難防護近乎免費,偵測開銷可依 CRC 粒度解析權衡。)

---

## 4. 分析(依公式:挑一兩個 component、把易變者設為變數)

**Scrub 間隔與錯誤率的解析關係。**設 per-bit 錯誤率 p、結構位元數 n,元素級損壞比例 f(t) = 1−(1−p)^{n·t/T}。懸崖層:以 4 位元累積閾值反推最大安全 scrub 間隔 t_cliff(p);斜坡層:給定 recall 底線 R,由實測容忍曲線 f\*(R) 反推重載間隔,並得間隔比 t_EB/t_drop = ln(1−f\*_EB)/ln(1−f\*_drop)。解析模型之三項輸入(p、結構位元數 n、R)均為先驗物理量,與量測首失敗分布量級一致(median 17.7 vs 21)——以合理 p 值代入製表【表:p ∈ 10⁻⁶…10⁻³ 下的 t_cliff 與間隔比】,insight:懸崖層即使在 p=10⁻³ 仍僅需低頻 scrub(64 B 太小),而斜坡層的重載頻率主要由 R 決定——保護成本對晶片品質的敏感度極低,是打撈可行的解析依據。

**偵測開銷隨 CRC 粒度可解析權衡。**每元素 16 B manifest 為索引的 5.7%,CRC32/元素為 1.4%,4 KB page 級為 ~0.03% 但放大 fallback 比例 ~43×、等比縮短安全重載間隔——低錯誤率下 f_page ≈ 43·f_elem,粒度甜蜜點為 p 之函數(此為被砍實驗 D 的解析版:偵測厚利、粒度換取偵測開銷與重載頻率之權衡)。

---

## 5. 實驗(依公式)

### 5.1 設定
Metrics:recall@10(相對 clean 0.98376)、沉默崩潰(保留率 <50%)、保護記憶體開銷、scrub/重載頻率。比較對象:無保護、drop(偵測後跳過)、none(不偵測)、均勻 3× 複製(解析)、AN-coding 開銷(解析,依 A 之選擇 15–100%+)、現行重載實務。平台:x86-64 工作站、RaBitQ-HNSW b=7、SIFT1M、seed 固定、故障注入於序列化位元組(單一注入來源)。

### 5.2 實驗結果
1. **(主結果,objective 的 metric)懸崖端 timeline**:無保護 6 bits 即沉默崩潰(0.984→0.423,100% silent)vs 三複本每查詢修 100 週期全程 0.98376 vs 複本不 scrub 的保險絲失效(seed 1014,首失敗於 tick 21 = 分布 median,與 30-seed 分布實驗中該 seed 之記錄逐值一致——同 lane 決定性驗證;失效後沿無保護軌跡崩至 0)vs 複本自我 scrub 撐住【圖:四線 timeline——本文最重要的一張圖;四線同 SeedSequence lane、同注入管線,唯一差異為防護配置】。
2. **斜坡端**:EB vs drop vs none 跨損壞比例(4 種空間模式曲線重合→空間幾何對斜坡無感,僅比例重要);none 的反轉(輕損傷最佳 0.906、重損傷沉默崩潰 0.127)→ 偵測買的是知情權;間隔比表(1.06–1.23×)與偵測 29× 容忍。
3. **Supporting**:成本 breakdown(懸崖端增量 132 B vs 均勻 3× 的 +544 MB vs AN-coding 的 15%+)、counter 於服務路徑即產出損壞比例 f(構造驗證,by construction,非偵測準確率的實驗發現)、多 seed 首失敗分布(N=30,median 21、IQR [9, 30])、跨量化器對照(SQ8 sq_scale 急性 56.2% vs 旋轉慢性)【stretch】。
(每張圖依公式:先講圖與軸→大趨勢→我們好在哪→例外與有趣觀察,如 none 反轉、XOR 互消。)

---

## 6. Conclusion
(依公式:不談背景;重述方法含全文細節——六個數量級、懸崖/斜坡、保險絲與自我 scrub(不 scrub 為一次性保險絲,首失敗 median ~21 週期;事件觸發重載使可再生,災難防護 132 B)、on-access 偵測對偶、災難防護 4.7×10⁻⁵% + 偵測層依 CRC 粒度 0.03–5.7% 的分層開銷、打撈 enable;future work 一兩點:weak-cell-aware placement 的實作、真實降 refresh 硬體驗證、多索引家族與 parity 擴展。)

---

## 附:related work 對照表(可置於 2.2 或第三章前,或精簡入文)

| 方法 | 保護目標 | 開銷分配 | 偵測後 | 於打撈場景的缺口 |
|---|---|---|---|---|
| ECC/Chipkill | 所有位元(透明) | 均勻 ~12.5%,不可選擇性省 | 硬體糾 1 bit;超出即 MCE | 設計點為零錯誤顆粒;p=10⁻³ 超出假設;無法利用重要性差異 |
| AHEAD(AN-coding) | 查詢處理之計算正確性 | 均勻(全欄同一 A);隨**錯誤率**可調 | 偵測;恢復交上層 | 無**重要性**軸;精確語意無近似自由度;偵測後 policy 缺席 |
| 均勻 3×/RS | 所有資料 | 均勻 200%/~30% | 多數決/解碼 | 為佔比極小(~10⁻⁵%)的臨界結構付全額;複製不自我 scrub 仍是保險絲 |
| Flikker/近似儲存 | 依應用標註之關鍵區 | 二分(關鍵/非關鍵) | — | 需人工標註;未及 ANN 索引;無查詢期恢復 |
| PVF(Meta) | 嵌入表容忍度**量測** | —(量測,非保護) | — | 止於量測,未做分配與恢復 |
| Milvus 實務 | 儲存層完整性 | checksum 儲存層 | 整段重載 | 記憶體層無逐查詢檢查;沉默崩潰不可見 |
| **本文** | **recall(服務品質)** | **按實測危險度分層** | **分層:自 scrub 多數決/EB 容忍+計數+批次重載** | 單索引家族、模擬故障(見 limitation) |

---

## 給 advisor 的批註請求(隨稿附上)
1. Abstract 第一句的「打撈 binned DRAM」經濟前提,強度是否恰當?
2. Intro 第三段的四類分類法(硬體均勻→軟體均勻→他域危險度感知→VDB 實務)是否成立、順序對否?
3. 「保險絲」發現(複製≠安全)放 abstract 第三句與主結果圖,份量對否?
4. 第四章主圖選「懸崖四線 timeline」而非 frontier,同意否?
5. AHEAD 以解析比較(不復現實作)是否足夠?
